//Escreva uma função recursiva que receba um valor inteiro em base decimal e o imprima em base binária.
#include <stdio.h>
    void PassaParaBinario (int valor){
        if (n<0)
        return n;
        else (quociente )
    }

